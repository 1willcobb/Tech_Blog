<section class="card">
    <div class="card_header">
        <h2>title</h2>
        <p>posted by {{user}} on {{date}}</p>
    </div>
    <div class="card_body">
        {{ text }}
    </div>
</section>