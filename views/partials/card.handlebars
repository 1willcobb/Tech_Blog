<section class="card glassy">
    <div class="card_header">
        <h2>title {{title}}</h2>
        <p>posted by {{user}} on {{date}}</p>
    </div>
    <div class="card_body">
        text here{{ text }}
    </div>
</section>